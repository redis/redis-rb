language: ruby

rvm:
  - 2.3.0

gemfile: ".travis/Gemfile"

sudo: false

env:
  global:
    - VERBOSE=true
    - TIMEOUT=1
    - TESTOPTS="-v"
  matrix:
    - conn=synchrony REDIS_BRANCH=2.8

branches:
  only:
    - master
